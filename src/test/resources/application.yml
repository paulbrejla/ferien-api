spring:
 application:
    name: holidays-api
 mcv:
     pathmatch:
       matching-strategy: ant_path_matcher
 h2:
    console:
      enabled: false
      path: /h2-console
server:
  port: 80
loader:
  source: filesystem
  remoteURL:
  branch: master
  filePath: